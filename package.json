{
  "name": "snyk-test-project",
  "version": "1.0.0",
  "private": true,
  "description": "Baseline-clean version of the sample project, for testing the dev security gate against a single newly-introduced issue rather than pre-existing ones.",
  "scripts": {
    "start": "echo 'This project is for scanning only, not for running'"
  },
  "dependencies": {
    "lodash": "4.18.1",
    "axios": "0.32.0",
    "express": "4.22.0",
    "minimist": "1.2.8",
    "jsonwebtoken": "9.0.2",
    "node-fetch": "2.7.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
